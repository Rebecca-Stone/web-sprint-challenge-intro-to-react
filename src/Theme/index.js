// Theme object

// Decided to stick with the styles used in header, for the color and textShadow.  
const theme = {
  primaryColor: "rgba(244, 165, 96, 0.603)",
  color: "#443e3e",
  textShadow: "1px 1px 5px #fff",
};

export default theme;